--[[
  ${CONKY_SUITE_DIR}/widgets/pfsense-conky.conf
  pfSense centered arc widget (Lua-drawn).
]]

local SUITE_DIR = os.getenv("CONKY_SUITE_DIR") or (os.getenv("HOME") .. "/.config/conky/gtex62-tech-hud")
local theme = dofile(SUITE_DIR .. "/theme.lua")
local pos = theme.layout_pos("pfsense")

conky.config = {
  alignment              = 'middle_middle',
  xinerama_head          = theme.monitor_head,
  background             = false,
  double_buffer          = true,
  update_interval        = 1,

  use_xft                = true,
  own_window             = true,
  own_window_type        = 'desktop',
  own_window_hints       = 'undecorated,sticky,skip_taskbar,skip_pager,below',
  own_window_argb_visual = true,
  own_window_argb_value  = 0,
  own_window_transparent = true,
  own_window_class       = 'Conky',

  minimum_width          = theme.layout_dim("pfsense", "min_w", 1200),
  maximum_width          = theme.layout_dim("pfsense", "max_w", 1200),
  minimum_height         = theme.layout_dim("pfsense", "min_h", 1200),

  gap_x                  = pos.x,
  gap_y                  = pos.y,

  draw_shades            = false,
  draw_borders           = false,
  default_color          = 'FFFFFF',

  lua_load               = SUITE_DIR .. "/lua/widgets/pf_widget.lua",
  lua_draw_hook_pre      = 'pf_draw',
}

conky.text = [[
# Lua-drawn widget (intentionally no Conky text output)
]]
