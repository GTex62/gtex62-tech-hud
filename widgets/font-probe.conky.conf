--[[
  ${CONKY_SUITE_DIR}/widgets/font-probe.conky.conf
  Eurostile font probe widget (standalone).
]]

local SUITE_DIR = os.getenv("CONKY_SUITE_DIR") or (os.getenv("HOME") .. "/.config/conky/gtex62-tech-hud")

conky.config = {
  alignment              = 'top_left',
  background             = false,
  double_buffer          = true,
  update_interval        = 5,

  use_xft                = true,
  own_window             = true,
  own_window_type        = 'desktop',
  own_window_hints       = 'undecorated,sticky,skip_taskbar,skip_pager,below',
  own_window_argb_visual = true,
  own_window_argb_value  = 0,
  own_window_transparent = true,
  own_window_class       = 'Conky',

  minimum_width          = 2400,
  maximum_width          = 2400,
  minimum_height         = 1400,

  gap_x                  = 30,
  gap_y                  = 30,

  draw_shades            = false,
  draw_borders           = false,
  default_color          = 'C0C0C0',

  lua_load               = SUITE_DIR .. "/lua/widgets/font_probe.lua",
  lua_draw_hook_pre      = 'draw_font_probe',
}

conky.text = [[

]]
