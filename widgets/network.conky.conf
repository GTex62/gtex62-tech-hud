--[[
  ${CONKY_SUITE_DIR}/widgets/network.conky.conf
  Network circle widget (throughput + ping meters).
]]

local SUITE_DIR = os.getenv("CONKY_SUITE_DIR") or (os.getenv("HOME") .. "/.config/conky/gtex62-tech-hud")

conky.config = {
  alignment              = 'top_left',
  background             = false,
  double_buffer          = true,
  update_interval        = 1,

  use_xft                = true,
  own_window             = true,
  own_window_type        = 'desktop',
  own_window_hints       = 'undecorated,sticky,skip_taskbar,skip_pager,below',
  own_window_argb_visual = true,
  own_window_argb_value  = 0,
  own_window_transparent = true,
  own_window_class       = 'Conky',

  minimum_width          = 400,
  maximum_width          = 400,
  minimum_height         = 400,

  gap_x                  = 4292,
  gap_y                  = 285,

  draw_shades            = false,
  draw_borders           = false,
  default_color          = 'C0C0C0',

  lua_load               = SUITE_DIR .. "/lua/widgets/sitrep.lua",
  lua_draw_hook_pre      = 'draw_network',
}

conky.text = [[
]]
