--[[
  ${CONKY_SUITE_DIR}/widgets/system.conky.conf
  System circle widget (season-tinted ring).
]]

local SUITE_DIR = os.getenv("CONKY_SUITE_DIR") or (os.getenv("HOME") .. "/.config/conky/gtex62-tech-hud")
local theme = dofile(SUITE_DIR .. "/theme.lua")
local pos = theme.layout_pos("system")

conky.config = {
  alignment              = 'top_left',
  xinerama_head          = theme.monitor_head,
  background             = false,
  double_buffer          = true,
  update_interval        = 1,

  use_xft                = true,
  own_window             = true,
  own_window_type        = 'desktop',
  own_window_hints       = 'undecorated,sticky,skip_taskbar,skip_pager,below',
  own_window_argb_visual = true,
  own_window_argb_value  = 0,
  own_window_transparent = true,
  own_window_class       = 'Conky',

  minimum_width          = theme.layout_dim("system", "min_w", 400),
  maximum_width          = theme.layout_dim("system", "max_w", 400),
  minimum_height         = theme.layout_dim("system", "min_h", 400),

  gap_x                  = pos.x,
  gap_y                  = pos.y,

  draw_shades            = false,
  draw_borders           = false,
  default_color          = 'C0C0C0',

  lua_load               = SUITE_DIR .. "/lua/widgets/sitrep.lua",
  lua_draw_hook_pre      = 'draw_system',
}

conky.text = [[
]]
