--[[
  ${CONKY_SUITE_DIR}/widgets/sitrep.conky.conf
  SITREP status panel (no arcs).

  • Uses theme.lua for layout and colors.
  • Uses ${CONKY_SUITE_DIR}/lua/widgets/sitrep.lua for drawing.
]]
-- Uses shared theme.lua for fonts/colors/layout
-- local theme = dofile(os.getenv("CONKY_SUITE_DIR") .. "/theme.lua")

local SUITE_DIR = os.getenv("CONKY_SUITE_DIR") or (os.getenv("HOME") .. "/.config/conky/gtex62-tech-hud")
local theme = dofile(SUITE_DIR .. "/theme.lua")
local pos = theme.layout_pos("sitrep")
SITREP_THEME = SUITE_DIR .. "/theme-sitrep.lua"

conky.config = {
  alignment              = 'top_left',
  xinerama_head          = theme.monitor_head,
  background             = false,
  double_buffer          = true,
  update_interval        = 1,

  use_xft                = true,
  -- font                   = font_notes,

  own_window             = true,
  own_window_type        = 'desktop',
  own_window_hints       = 'undecorated,sticky,skip_taskbar,skip_pager,below',
  own_window_argb_visual = true,
  own_window_argb_value  = 0,
  own_window_transparent = true,
  own_window_class       = 'Conky',

  minimum_width          = theme.layout_dim("sitrep", "min_w", 460),
  maximum_width          = theme.layout_dim("sitrep", "max_w", 560),
  minimum_height         = theme.layout_dim("sitrep", "min_h", 800),

  gap_x                  = pos.x,
  gap_y                  = pos.y,

  draw_shades            = false,
  draw_borders           = false,
  default_color          = 'C0C0C0',
  color1                 = '808080',

  lua_load               = SUITE_DIR .. "/lua/widgets/sitrep.lua",
  lua_draw_hook_pre      = 'draw_sitrep',
}

conky.text = [[
]]
