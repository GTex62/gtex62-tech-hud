# OpenWeather variables (used by the fetch script)
LAT=YOUR_LAT        # e.g., 41.878100 (Chicago, IL latitude)
LON=YOUR_LON        # e.g., -87.629800 (Chicago, IL longitude)
UNITS=imperial      # imperial|metric|standard
LANG=en             # ISO 639-1 language code
CACHE_TTL=300       # seconds to reuse cached data
METAR_TTL=600       # seconds to reuse cached METAR
TZ=America/Chicago  # IANA timezone (e.g., America/New_York, Europe/London, Europe/Berlin)
# Note: timezone changes take effect after the cache TTL expires.

# Cache override (optional; otherwise defaults will be used)
OWM_DAILY_CACHE=$CONKY_CACHE_DIR/owm_forecast.json
SEASONAL_TTL=31536000  # seconds to reuse seasonal cache (1 year)
SEASONAL_CACHE=$CONKY_CACHE_DIR/seasonal.vars
